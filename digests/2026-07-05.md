# 每日论文简报 · 2026-07-05

**今日无高相关论文（无新批次，周末公告断档第二天）。**

当日总览：今天（周日）复扫 arXiv 三条主分类 feed（cs.LG / cs.AI / cs.CL，各取 120 条，`sortBy=submittedDate` 降序，直连 `export.arxiv.org/api`），去重后 **288 篇**，其中 236 篇 `published` 为 2026-07-02、52 篇为 07-01——**最新提交日仍停在 2026-07-02，最大 ID 仍为 `2607.02514`**，与昨天 07-04 简报的上界完全一致。也就是说自 07-03 简报以来，连续第二天**没有出现比 07-02 更新的提交批次**。而 07-02 这批昨昨天（07-03 简报）已完整挖过、6 篇入选（TPP 单调交替样条 `01752`、自解释算子学习 `02203`、小波异常检测 `02046`、脑网络 SABER `01901`、自修复电路 CoAx `01940`、循环表征几何 `01746`），次优候选（EEG 抑郁识别双曲 GNN `02063`）也已作为 also-ran 在案。今天没有属于我方向的**新**高相关论文，故按"保持每日记录完整"的约定留此空档记录。这是周末 arXiv 公告的正常断档：07-05 为周日，周四截稿的批次已在上一工作日消化完，新一批预计要到 **07-06（周一）/ 07-07（周二）** 才会出现。

**与昨天的差异（数据源侧的一点好消息）**：昨天 07-04 简报里反复触发 429/503 而未能取回的**跨分类专题检索**，今天全部跑通了——temporal graph / dynamic graph、fNIRS / brain network、anomaly detection / root cause / AIOps、irregularly-sampled / Neural ODE / temporal point process、self-explaining / information bottleneck / prototype 共 5 条定向查询（覆盖 eess.SP、q-bio、cs.CR/NI/SE），各取 40 条均正常返回（HTTP 200）。这些专题结果**同样以 07-02 为最新提交日上界**，没有任何 07-02 之后的专题-only 命中。因此"无新批次"这个结论今天是由主 feed + 专题检索**双重确认**的，比昨天仅凭主 feed `submittedDate` 上界的判断更稳。

**领域整体动向（基于复扫的 07-02 存量批次，非新论文，仅记录趋势）**：延续昨日观察，LLM agent 侧的 **memory / 长期记忆机制**仍是本周批次里最密集、也最快同质化的一条线（记忆巩固与身份漂移、状态感知失效解耦、线性注意力的精确记忆补丁、多头循环记忆等同题工作扎堆），正对我 C 方向"优先"口径的机制/架构问题，但多为增量或工程向，单篇未达入选门槛；**机制可解释**方向的活水（稀疏字典参数高效化、检索头 logit 贡献定位、表征相似度观测台）延续 CoAx `01940` 那种"给机制归因方法学纠偏"的势头。**A 核心方法**方向本周新增趋近于零，**B 应用**方向的 **fNIRS 仍无直接命中**（存量里最接近的仍是 EEG/脑网络的 `02063`/`01901`，均已处理）。总体判断：本周批次已彻底进入尾声消化期，下一个工作日批次（07-06/07-07）到来时，重点看 agent-memory 这条线是否有把"记忆 / 表征几何"真正上升到机制层面的技术报告类工作，以及 A 方向（时序图 / 连续时间 / 内生自解释）是否有新供给。

---

## 数据源说明

- **主分类 feed（已成功抓取）**：cs.LG（285,616 B）、cs.AI（285,156 B）、cs.CL（286,125 B），HTTPS 直连 `export.arxiv.org/api/query`，`max_results=120`，`sortBy=submittedDate` 降序，各返回 120 条，去重 288 篇。最新提交日 = 2026-07-02，最大 ID = `2607.02514`，据此判定"无更新批次"。
- **跨分类专题检索（今日已成功抓取）**：temporal/dynamic graph、fNIRS/brain network/neuroimaging、anomaly detection/root cause/AIOps、irregularly-sampled/Neural ODE/temporal point process、self-explaining/information bottleneck/prototype 共 5 条 all-field 查询，各 `max_results=40`，均 HTTP 200 正常返回。结果的最新提交日同样为 2026-07-02，无 07-02 之后的专题-only 命中——与主 feed 相互印证"无新批次"。
- 标题、arXiv ID、日期均来自实抓数据，未编造；本文所引 ID 均属已抓到的 07-02 存量批次，仅用于说明领域动向，非今日新入选。
