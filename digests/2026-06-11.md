# 每日论文简报 · 2026-06-11

> 今日总览：**未能抓取到论文** —— 本次运行所在的执行环境网络策略（白名单模式）未放行 arXiv 及各备选论文源，唯一可用的数据通道无法获取可验证的论文元数据，故今日无可筛选的论文。下方记录本次（UTC 12:50 复跑）的排查结果，便于修复后恢复每日抓取。

## 今日无高相关论文

今天没有产出任何论文条目，原因是**数据源抓取失败**，而非"当天该领域无新论文"。为遵守"标题/作者/arXiv ID 必须来自真实抓取数据、不得编造"的约束，这里不填充任何论文。

### 抓取失败说明（数据源排查）

本次运行的容器网络为**白名单（allowlist）模式**，仅放行 GitHub 与 PyPI，论文相关的数据源全部被代理拦截。本次复跑用 `curl` 与 `WebFetch` 逐一探测，结果如下：

| 数据源 | curl / WebFetch 结果 |
|---|---|
| `export.arxiv.org/api/query`（arXiv 官方 API，主数据源） | ❌ 403 / Host not in allowlist |
| `arxiv.org` / `arxiv.org/list/cs.LG/new`（arXiv 站点） | ❌ 403 / Host not in allowlist |
| `rss.arxiv.org`（arXiv RSS） | ❌ 403 / Host not in allowlist |
| `api.semanticscholar.org`（备选） | ❌ 403 / Host not in allowlist |
| `api.openalex.org`（备选） | ❌ 403 / Host not in allowlist |
| `huggingface.co/papers`（备选） | ❌ 403 / Host not in allowlist |
| `github.com`（对照组） | ✅ 可达（WebFetch 成功返回页面） |

`curl` 与 `WebFetch` 走同一套出站白名单：对照组 `github.com` 可正常访问，而上述所有论文源均返回 403 / "Host not in allowlist"，确认是**网络策略拦截**而非数据源本身故障。

### 为什么没有改用 WebSearch 兜底

`WebSearch` 工具本次是**可用**的（走 Anthropic 侧检索，不受容器白名单限制），但它**不能替代** arXiv 抓取，故本次未据其产出论文条目：

- **时效不达标**：搜索索引存在数天至数周滞后，无法稳定命中"过去约 24 小时新提交 / 更新"的论文；本次试搜返回的多为往月（如 ID `2602.xxxxx` 即 2026-02）或更早论文。
- **元数据不可核**：搜索摘要通常缺少完整作者列表，且无法打开 arXiv abstract 页核对标题 / 作者 / arXiv ID；据此填表会违反"不得编造"的硬约束。

因此按规则：数据源抓取失败 → 跳过该源并注明，不用低可信来源强行填充。

### 该领域整体动向（不含今日具体论文）

无新数据可依，仅作方向性提示、不指向任何具体新论文：时序图 / CTDG、不规则采样时间序列（Neural / latent ODE）、内生可解释（信息瓶颈、原型 / 概念）以及 AIOps 根因定位等方向近期持续活跃，待网络恢复后应优先回补这几类的近 24–48 小时新提交。

### 修复建议（需在环境配置层面处理，Claude 无法自行放行）

要恢复每日抓取，请在创建 / 编辑该 Claude Code on the web 环境时，将网络策略改为允许出站访问，或在白名单中加入以下域名：

- `export.arxiv.org`（arXiv API，**必需**）
- `arxiv.org`、`rss.arxiv.org`（arXiv 站点 / RSS）
- 可选备选源：`api.semanticscholar.org`、`api.openalex.org`、`huggingface.co`

网络策略的配置说明见官方文档：https://code.claude.com/docs/en/claude-code-on-the-web

---
*抓取失败的数据源：arXiv API / 站点 / RSS、Semantic Scholar、OpenAlex、HuggingFace（均因白名单未放行被拦截，本次复跑 403 确认）。本文件按"每日记录完整"的约定生成，待网络放行后即可恢复正常抓取与筛选。*
