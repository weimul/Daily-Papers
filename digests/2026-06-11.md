# 每日论文简报 · 2026-06-11

> 今日总览：**未能抓取到论文** —— 本次运行所在的执行环境网络策略未放行 arXiv，唯一的数据源抓取失败，故今日无可筛选的论文。下方记录排查情况，便于修复后恢复每日抓取。

## 今日无高相关论文

今天没有产出任何论文条目，原因是**数据源抓取失败**，而非"当天该领域无新论文"。为遵守"标题/作者/arXiv ID 必须来自真实抓取数据、不得编造"的约束，这里不填充任何论文。

### 抓取失败说明（数据源排查）

本次运行的容器网络为**白名单（allowlist）模式**，仅放行 GitHub 与 PyPI，论文相关的数据源全部被代理拦截：

| 数据源 | 结果 |
|---|---|
| `export.arxiv.org/api/query`（arXiv 官方 API，主数据源） | ❌ Host not in allowlist |
| `rss.arxiv.org` / `export.arxiv.org/rss`（arXiv RSS） | ❌ Host not in allowlist |
| `arxiv.org` / `www.arxiv.org` | ❌ Host not in allowlist |
| `api.semanticscholar.org`（备选） | ❌ Host not in allowlist |
| `api.openalex.org`（备选） | ❌ Host not in allowlist |
| `huggingface.co/papers`（备选） | ❌ Host not in allowlist |
| `github.com` / `pypi.org` | ✅ 可达（但无法替代论文检索） |

WebFetch 工具与命令行 `curl` 走的是同一套白名单，均返回拦截。因此本次没有任何真实论文数据可用于筛选与归纳。

### 该领域整体动向（不含今日具体论文）

无新数据可依，故仅作方向性提示、不指向任何具体新论文：时序图 / CTDG、不规则采样时间序列（Neural/latent ODE）、内生可解释（信息瓶颈、原型/概念）以及 AIOps 根因定位等方向近期持续活跃，待网络恢复后应优先回补这几类的近 24–48 小时新提交。

### 修复建议（需在环境配置层面处理，Claude 无法自行放行）

要恢复每日抓取，请在创建/编辑该 Claude Code on the web 环境时，将网络策略改为允许出站访问，或在白名单中加入以下域名：

- `export.arxiv.org`（arXiv API，**必需**）
- `arxiv.org`、`rss.arxiv.org`（arXiv 站点 / RSS）
- 可选备选源：`api.semanticscholar.org`、`api.openalex.org`、`huggingface.co`

网络策略的配置说明见官方文档：https://code.claude.com/docs/en/claude-code-on-the-web

---
*抓取失败的数据源：arXiv API / RSS、Semantic Scholar、OpenAlex、HuggingFace（均因白名单未放行被拦截）。本文件按"每日记录完整"的约定生成，待网络放行后即可恢复正常抓取与筛选。*
